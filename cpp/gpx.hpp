#include <filesystem>

double tinyxml_gpx_length(const std::filesystem::path& path);
// double tinyxml_gpx_length(const std::string& gpxdata);

double pugixml_gpx_length(const std::filesystem::path& path);
